<h1 class="text-center">Create new review</h1>

<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :content %>
  <%= f.input :rating, collection: (0..5).to_a, prompt: "Choose a number between 0 and 5" %>
  <%= f.submit "Create review" %>
<% end %>
